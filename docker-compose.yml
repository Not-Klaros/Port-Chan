version: "3.8"

services:
  web:
    build:
      context: ./my-portfolio  # Change if your Dockerfile isn't in this subfolder
    container_name: vite-portfolio-prod
    ports:
      - "80:3000"  # 'serve' defaults to port 3000 internally
    restart: unless-stopped
